# Demo objects

The `travel/` directory contains six fictional sample objects for screenshots and local S3 testing: five generated scenery images and a three-second video made from one of them. They contain no user data or credentials.

Run [`scripts/seed-demo.sh`](../../scripts/seed-demo.sh) with your own disposable bucket to upload them. The script writes only these six filenames under the chosen prefix.
